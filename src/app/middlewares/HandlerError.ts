import { NextFunction ,Response, Request } from "express";
import { ResponseUtil } from "../../utils/Response";

export class HandlerError{
    static handlerError(err : any,req:Request, res:Response, next : NextFunction){

    }
}